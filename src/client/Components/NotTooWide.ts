import styled from 'styled-components';

import {
  mediumScreenMax,
} from '../theme';

export const NotTooWide = styled.div`
  margin: 0 auto;
  max-width: ${mediumScreenMax};
`;

export default NotTooWide;
